# EC WebApp レビュー結果

## 概要

本レビューでは、当該プロジェクトを以下の 8 観点で確認した。

1. ディレクトリ・ファイル構成
2. 命名規則
3. 設計・処理の分割方法
4. コーディングスタイル・可読性
5. エラーハンドリング
6. セキュリティ対策
7. 処理効率
8. テストのしやすさ

全体として、レイヤ構成は比較的明確であり、Controller / Service / Repository の分離方針も保たれている。一方で、継続的に機能追加・保守する前提では、セキュリティ、例外設計、状態管理、効率面にいくつか改善余地がある。

---

## レビュー結果

| 重要度 | 観点 | 課題 | 改善方法 | 改善理由 |
|---|---|---|---|---|
| 高 | セキュリティ対策 | H2 コンソールが常時有効で、SQL 初期化も常時実行、ログレベルも DEBUG のままになっている。さらに Spring Security が導入されておらず、CSRF や認可の仕組みが前提になっていない。 | 開発用と本番用でプロファイルを分離し、H2 コンソール・SQL 初期化は開発環境のみに限定する。ログレベルも本番では INFO 以上に下げる。公開運用を前提とする場合は Spring Security を導入し、CSRF・認証・認可を有効化する。 | 管理機能の露出、意図しないデータ初期化、不要な内部情報の出力を防げる。今後ユーザアカウント機能を追加する際にも必須になる。 |
| 高 | エラーハンドリング | 注文確定処理で注文登録とカート削除を行っているが、トランザクション境界がない。また IllegalArgumentException を業務例外として流用し、リクエスト URI で処理分岐している。 | OrderService の注文確定処理にトランザクションを設定する。空カートや数量不正などは専用の業務例外クラスを作成し、GlobalExceptionHandler では URI ではなく例外型で分岐する。 | 途中失敗時のデータ不整合を防げる。例外の意味が明確になり、画面や機能追加時の副作用が減る。 |
| 高 | 設計・処理の分割方法 | カート状態をセッションと DB の両方で管理しており、商品一覧系ではセッション、カート画面では DB を見ている。状態の正本が一意でない。 | カート情報の正本をセッションか DB のどちらかに統一する。将来的な会員機能や永続カートまで考慮するなら、ユーザまたはセッション ID に紐づく DB 管理へ寄せる。 | 表示件数と実データの不整合を避けられる。将来のログイン機能、複数端末、履歴連携にも対応しやすい。 |
| 中 | 処理効率 | CartService が同じカート情報を複数回取得し、それを Controller 側でも個別に呼び出しているため、画面表示中に同系統の DB アクセスが重複しやすい。 | 画面表示用の集計結果をひとつのモデルにまとめる。CartService 内で 1 回取得したカート一覧から件数、商品合計、送料、割引額、請求額を算出する構成にする。 | DB アクセス回数を削減できる。金額計算の一貫性も高まり、仕様変更時の修正箇所も減る。 |
| 中 | テストのしやすさ | Controller テストや Repository テストの一部が SpringBootTest でフルコンテキスト起動になっており、対象に対して重い。 | Controller は WebMvcTest、Repository はスライステスト、Service は Mockito ベースの単体テストに寄せる。フル統合テストは必要最小限にする。 | テスト速度が上がり、失敗時の原因切り分けがしやすくなる。開発中のフィードバックも早くなる。 |
| 中 | 命名規則 | orders テーブルに adress というスペルミスがある。また totalAmount が画面によって「商品合計」と「請求合計」の両方の意味で使われており、意味が揺れている。 | カラム名を address に統一する。金額系の命名も itemTotalAmount、shippingAmount、discountAmount、billingAmount など、意味単位で分離する。 | 命名の意味が明確になり、誤読や修正漏れを減らせる。DB と Java コードの対応も追いやすくなる。 |
| 低 | ディレクトリ・ファイル構成 | mock 配下の画面 HTML と templates 配下の実テンプレートが別管理になっており、UI の正本が分かれやすい。 | mock は docs 配下へ移すか、参照専用であることを明示する。実運用上の正本は templates に統一する。 | どちらを修正対象とすべきかが明確になり、画面変更時の混乱を減らせる。 |
| 低 | コーディングスタイル・可読性 | Controller に画面モデルの組み立て、入力受付、分岐、初期化処理が増えており、表示要件の追加に伴って肥大化しやすい。 | 画面表示用の ViewModel 組み立てを Service あるいは専用アセンブラへ寄せる。Controller は入力処理と画面遷移の判断に集中させる。 | 責務が整理されて可読性が上がる。表示項目や入力欄が増えたときにも保守しやすい。 |

---

## 総評

本プロジェクトは、教材や小規模サンプルとしては十分に読みやすく、レイヤ構造も比較的明快である。  
一方で、今後ユーザアカウント機能や注文履歴、永続カートなどを追加していく場合、以下の点を優先的に改善するのが望ましい。

1. セキュリティ設定の本番対応
2. 例外設計とトランザクション管理の整理
3. カート状態管理の一元化
4. 集計処理の重複削減

これらを先に整えることで、以後の機能追加コストと障害リスクを大きく下げられる。

---

## 補足

今回のレビューは、講義用の最小構成というより、今後も保守・拡張される Java Web アプリケーションとして評価した内容である。  
そのため、動作していること自体よりも、保守性・拡張性・安全性の観点を重視して指摘している。